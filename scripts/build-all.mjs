console.error("Cross-platform installers are built on their target operating system.");
console.error("Use GitHub Actions > Build release to build Windows, Linux and macOS x64 artifacts.");
process.exit(1);
